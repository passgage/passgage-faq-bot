# Cloudflare Workers Environment Variables
# Copy this file to .env for local development
# DO NOT commit .env to version control

# Optional: Override default thresholds for local testing
# SIMILARITY_THRESHOLD=0.7
# TOP_K=3
# MAX_FAQs_RETURN=5

# Security: Two-tier API Key System
# Generate strong random keys: openssl rand -hex 32

# PUBLIC API KEYS - For frontend/mobile app (read-only access to /api/ask)
# Give this to your Passgage application
# PUBLIC_API_KEYS=public_key_1,public_key_2

# ADMIN API KEYS - For admin operations (create, delete, seed FAQs)
# Keep this secret, use only for backend/admin panel
# ADMIN_API_KEYS=admin_key_1,admin_key_2

# CORS: Allowed origins (comma-separated, or * for all)
# ALLOWED_ORIGINS=https://passgage.com,https://app.passgage.com

# IP Whitelist: Comma-separated list of allowed IPs (optional)
# WHITELISTED_IPS=192.168.1.100,10.0.0.50

# Rate Limiting
# RATE_LIMIT_MAX=60

# Note: Cloudflare bindings (AI, VECTORIZE, RATE_LIMIT_KV) are configured in wrangler.toml
